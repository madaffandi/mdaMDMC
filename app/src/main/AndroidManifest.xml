<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="achmadaffandi.mdisaster">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="mDisaster"
            android:theme="@style/AppFullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateDisasterActivity"
            android:theme="@style/AppFullScreenTheme" />
        <activity
            android:name=".InitiateDisasterActivity"
            android:theme="@style/AppFullScreenTheme" />
        <activity
            android:name=".ForgetPassActivity"
            android:theme="@style/AppFullScreenTheme" />
        <activity
            android:name=".IntroActivity"
            android:theme="@style/AppFullScreenTheme" />
        <activity
            android:name=".DisListActivity"
            android:theme="@style/AppFullScreenTheme" />
        <activity
            android:name=".DashboardDisasterActivity"
            android:theme="@style/AppFullScreenTheme" />
        <activity
            android:name=".DisDetailActivity"
            android:theme="@style/AppFullScreenTheme" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/AppFullScreenTheme" />
    </application>

</manifest>